# リレーション

データベースのテーブル同士が、どのような関係になっているかを表す考え方。
代表的なものとして3つ。
- 1体1
- 1対多
- 多対多

## 1対1リレーション

1つのデータに対して、相手側のデータも1つだけ存在する関係のこと。

## 1対多リレーション

一つのテーブルの各レコードが別のテーブルの複数のレコードと関連付けられること。

## 多対多リレーション

例えばこのケース。
1人のユーザーが複数のタグを持てて、1つのタグにも複数のユーザーが存在する場合など。
```
User A
 ├─ Ruby
 ├─ Rails
 └─ Docker
```
```
Ruby
 ├─ User A
 ├─ User B
 └─ User C
```

# アソシエーション

Railsのモデル間の関連性を定義する機能。
アソシエーションを利用することで、異なるモデル同士がどのように結び付いているかを簡潔に表現できる。
主なアソシエーションとして以下3つ。
- `has_one`
- `has_many`
- `belongs_to`

### has_one

`has_one` アソシエーションは、このモデルは相手のモデルを1つ持っているという意味。
例えば、1人のユーザーが1つのプロフィールを持つ場合、Userモデルに対して次のように定義する。

```
has_one :profile
```
これにより、Userモデルのインスタンスから、と書くだけで、そのユーザーに紐づいたプロフィールを取得できる。

### has_many

`has_many` アソシエーションは、1つのモデルが複数の別モデルと関連付けられる場合に使用する。
例えば、1人のユーザーが複数の掲示板を持つ場合、Userモデルに対して次のように定義する。

```
has_many :boards
```

これにより、Userモデルのインスタンスから、そのユーザーが投稿した複数の掲示板情報を簡単に取得できる。

### belongs_to

`belongs_to`は、このBoardは1人のUserに属しているという意味。
例えば、掲示板がユーザーに属する場合、Boardモデルに対してUserモデルを belongs_to :user と定義する。
これにより、Boardモデルのインスタンスから投稿者であるユーザー情報を簡単に取得できます。

```
belongs_to :user
```


### アソシエーションを定義する理由・メリット

アソシエーションを定義することで、簡潔な記述によって関連レコードを呼び出せるようになる。
例えば、Boardモデルのインスタンスから、紐付いているユーザー情報を取得する場合、アソシエーションの有無によって記述方法が異なる。

#### **アソシエーションがない場合**

```ruby
class BoardsController < ApplicationController
  def show
    @board = Board.find(:id)
    @user = User.find(@board.user_id)
  end
end
```

このコードでは、`@board` オブジェクトが持つ `user_id` を取得し、その `user_id` を使ってUsersテーブルから該当するユーザーを検索している。

#### **アソシエーションがある場合**

```ruby
class BoardsController < ApplicationController
  def show
    @board = Board.find(params[:id])
    @user = @board.user
  end
end
```

このコードでは、`@board` オブジェクトが持つ `user` アソシエーションを直接呼び出し、関連するユーザーのレコードを取得している。
アソシエーションが定義されているため、Railsが内部的に適切なSQLクエリを生成し、関連するユーザーのレコードを取得する。
そのほかにも、一方のレコードが削除された際に、関連するもう一方のレコードも自動的に削除するよう設定できるなど、アソシエーションには大きなメリットがある。
そのため、必要なモデル間には適切にアソシエーションを定義することが重要となる。

# **バリデーション**

データがデータベースに保存される前に、そのデータが正しい形式や条件を満たしているかを確認する仕組み。
バリデーションを使用することで、不正なデータがデータベースに保存されることを防ぎ、アプリケーションの信頼性を高めることができる。
代表的なバリデーションとして以下3つ。
- `presence`
-  `length`
-  `uniqueness`

### **presence**

`presence` バリデーションは、指定された属性が空でないことを確認するために使用する。
例えば、ユーザー登録時に名前の入力を必須とする場合、モデルに次のように記述する。
これにより、名前が空の場合は保存に失敗し、エラーメッセージが表示される。

```
validates :name, presence: true
```


### **length**

`length` バリデーションは、指定された属性の長さが特定の条件を満たしているか確認するために使用する。
例えば、パスワードを8文字以上にする場合、モデルに次のように記述する。
これにより、パスワードが8文字未満の場合は保存に失敗する。
```
validates :password, length: { minimum: 8 }
```


### **uniqueness**

`uniqueness` バリデーションは、指定された属性の値が[^3]一意であることを確認するために使用する。
データベースやプログラミングの文脈では、ある属性の値が他のレコードと重複していない状態を指す。
例えば、ユーザー登録時にメールアドレスがすでに登録されていないことを確認する場合、モデルに次のように記述する。
これにより、同じメールアドレスがすでに登録されている場合は保存に失敗し、エラーメッセージが表示される。

```
validates :email, uniqueness: true
```

### dependent: :destroy

親モデルを削除するとその親モデルに紐づく「子モデル」も**一緒に削除できる


referencesは、外部キーを設定するためのメソッドです。このメソッドを使用することで、BoardsテーブルとUsersテーブルを関連付けることができます。また、foreign_key: true オプションを指定することで、外部キー制約を設定しています。

外部キー制約とは、あるテーブルのカラムの値が、他のテーブルの特定のカラムの値と一致することを強制するデータベースの仕組みです。この制約を設定することで、データベースの整合性を保ち、不整合なデータの挿入を防ぎます。例えば、Boards テーブルの user_id カラムに外部キー制約を設定することで、その user_id が必ず Users テーブルの id カラムに存在することを保証します。

●string型とtext型の違い

- string型
    - 長さ: 255文字までの文字列を扱います。
- text型
    - 長さ: 65,535文字までの文字列を扱います。

### データベースにマイグレーションファイルを適用する

以下のコマンドを実行してマイグレーションファイルをデータベースに適応してください。

```
$ docker compose exec web rails db:migrate
```

#### 適用したマイグレーションファイルを修正したい時

migrateしてup状態にあるマイグレーションファイルを修正したい場合は、下記の手順を行ってください。

- 現在のマイグレーション状態を確認する
    
    - 以下のコマンドを実行して、現在のマイグレーション状態を確認します。
    
    ```
    $ docker compose exec web rails db:migrate:status
    ```
    
    [![Image from Gyazo](https://t.gyazo.com/teams/startup-technology/54d6051bd92ba2656e5ed7cc5950feec.png)](https://startup-technology.gyazo.com/54d6051bd92ba2656e5ed7cc5950feec)
- 対象のマイグレーションをdownする
    - マイグレーションファイルに記載ミスがあった場合、まずそのマイグレーションをdown状態にする必要があるため、以下のコマンドを実行してください。
        
        ```
        $ docker compose exec web rails db:rollback STEP=1
        ```
        
        [![Image from Gyazo](https://t.gyazo.com/teams/startup-technology/74ff187ffa7d2068e2c52521864b9a31.png)](https://startup-technology.gyazo.com/74ff187ffa7d2068e2c52521864b9a31)
    - STEP=1 は、1つ前のマイグレーションに戻すことを意味します。複数のマイグレーションを戻す場合は必要なSTEP数を指定してください。
- マイグレーションファイルを修正する
    - down状態に戻した後、マイグレーションファイルの修正を行います。
- 再度マイグレーションを適用する
    - 修正が完了したら、再度マイグレーションを実行します。
        
        ```
        $ docker compose exec web rails db:migrate
        ```
        
        [![Image from Gyazo](https://t.gyazo.com/teams/startup-technology/4ef0fb0d46c6f0a21f54516c193566a3.png)](https://startup-technology.gyazo.com/4ef0fb0d46c6f0a21f54516c193566a3)
- マイグレーションの確認
    - 再度マイグレーション状態を確認して適用されたことを確認します。
        
        ```
        $ docker compose exec web rails db:migrate:status
        ```
        
        [![Image from Gyazo](https://t.gyazo.com/teams/startup-technology/57e14dd07aee1751fcd5bb67542b595d.png)](https://startup-technology.gyazo.com/57e14dd07aee1751fcd5bb67542b595d)

## ■掲示板一覧画面へのルーティング設定する

### config/routes.rbに resources :boards, only: %i[index] を記載する

`config/routes.rb`を以下のように編集してください。

- config/routes.rb

```
Rails.application.routes.draw do
  ... 省略 ...
  resources :boards, only: %i[index]
  .. 省略 ...
end
```

## ■設定したルーティングに対応するコントローラーとアクションを用意する

### docker compose exec web rails g controller boards コマンドを実行する

該当コマンドを実行してapp/controllers/boards_controller.rbを生成してください。

### app/controllers/boards_controller.rbにindexアクションを定義する

`app/controllers/boards_controller.rb`を以下のように編集してください。

- app/controllers/boards_controller.rb

```
class BoardsController < ApplicationController
  def index
    @boards = Board.includes(:user)
  end
end
```

includesメソッドは、関連するテーブルをまとめてDBから取得できるメソッドです. Board.includes(:user) は、Boardモデルのレコードと、それに関連するUserモデルのレコードを一度に取得します。これにより、最初のクエリでBoardレコードを取得し、2つ目のクエリで関連するUserレコードを一度に取得するため、クエリの発行回数を2回に抑えてN+1問題に対応しています。

[![Image from Gyazo](https://t.gyazo.com/teams/startup-technology/35280c2a157c3a30bceeacfb019726b9.png)](https://startup-technology.gyazo.com/35280c2a157c3a30bceeacfb019726b9)

N+1問題とは、SQLが必要以上に実行されてしまいパフォーマンスが落ちる問題です。SQLの実行とは、データベースに対してデータの読み書きや検索を行う命令を送信することを意味します。パフォーマンスが落ちるとは、アプリケーションが要求された操作を完了するまでにかかる時間が増加し、全体的な動作速度が遅くなることを意味します。

includesメソッドを使わず、Board.all で取得した場合、掲示板の投稿者名を表示する部分でN+1問題が発生します。

[![Image from Gyazo](https://t.gyazo.com/teams/startup-technology/76b7f9659b97a0ef3d07abd431096fde.png)](https://startup-technology.gyazo.com/76b7f9659b97a0ef3d07abd431096fde)

[![Image from Gyazo](https://t.gyazo.com/teams/startup-technology/758200e17bcabaec84de0090a86753c6.png)](https://startup-technology.gyazo.com/758200e17bcabaec84de0090a86753c6)

データが少ない場合は、クエリがたくさん発行されていてもあまり気にならないかもしれませんが、 データ量が増えてくると、処理が完了するまでにかかる時間も次第に長くなり、アプリケーションのパフォーマンスに悪影響を及ぼします。

## ■用意したコントローラーとアクションに対応するビューを用意する

### app/views/boards/index.html.erbを生成・編集する

`app/views/boards/index.html.erb`を生成して、以下のように編集してください。

- app/views/boards/index.html.erb

```
<div class="container pt-3">
  <div class="row">
    <div class="col-lg-10 offset-lg-1">
      <!-- 検索フォーム -->
      <form>
        <div class="input-group mb-3"><input class="form-control" placeholder="検索ワード" type="search"/>
          <div class="input-group-append"><input type="submit" value="検索" class="btn btn-primary"/></div>
        </div>
      </form>
    </div>
  </div>
  <!-- 掲示板一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @boards.present? %>
          <% @boards.each do |board| %>
            <div class="col-sm-12 col-lg-4 mb-3">
              <div id="board-id-<%= board.id %>">
                <div class="card">
                  <%= image_tag "board_placeholder.png", class: "card-img-top", width: "300", height:"200" %>
                  <div class="card-body">
                    <div class="d-flex">
                      <h4 class="card-title">
                        <%= link_to board.title, '#' %>
                      </h4>
                      <div class='ms-auto'>
                        <%= link_to '#', id: "button-edit-#{board.id}" do %>
                          <i class="bi bi-pencil-fill"></i>
                        <% end %>
                        <%= link_to '#', id: "button-delete-#{board.id}", method: :delete, data: {confirm: ''} do %>
                          <i class="bi bi-trash-fill"></i>
                        <% end %>
                      </div>
                    </div>
                    <ul class="list-inline">
                      <li><i class="bi bi-person"></i><%= board.user.decorate.full_name %></li>
                      <li><i class="bi bi-calendar"></i><%= l board.created_at, format: :short %></li>
                    </ul>
                    <p class="card-text">本文</p>
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        <% else %>
          <div class="mb-3">掲示板がありません</div>
        <% end %>
      </div>
    </div>
  </div>
</div>
```

●上記コードの解説

- if @boards.present?
    - @boards.present?は、@boardsに値が存在するかどうかを確認します。存在する場合はif以下を実行し、存在しない場合はelse以下を実行します。
- @boards.each do |board| から endまで
    - @boards.eachは、@boardsに含まれる全てのBoardオブジェクトをループ処理します。各Boardオブジェクトはboardとして処理されます。ループ内では、Boardの情報を表示するためのHTMLコードを生成します。
- else以下 <div class='mb-3'>掲示板がありません</div>
    - else以下の部分は、@boardsに値が存在しない場合に実行されます。この場合、「掲示板がありません」というメッセージを表示します。

### app/assets/images/配下にboard_placeholder.pngを配置する

以下のリンクを開いて画像をダウンロードした後、app/assets/images/配下に、ファイル名と拡張子を board_placeholder.png として配置してください。

[board_placeholder.png](https://user-images.githubusercontent.com/10973259/70017892-c40bbc00-15c7-11ea-8022-9cb7e5d22aa1.png)

### app/views/boards/_board.html.erbを生成・編集する

先程編集したapp/views/boards/index.html.erbの掲示板を繰り返し表示する部分を、保守性向上の観点から切り出します。`app/views/boards/_board.html.erb`を生成して、以下のように編集してください。

- app/views/boards/_board.html.erb

```
<div class="col-sm-12 col-lg-4 mb-3">
  <div id="board-id-<%= board.id %>">
    <div class="card">
      <%= image_tag "board_placeholder.png", class: "card-img-top", width: "300", height:"200" %>
      <div class="card-body">
        <div class="d-flex">
          <h4 class="card-title">
            <%= link_to board.title, '#' %>
          </h4>
          <div class='ms-auto'>
            <%= link_to '#', id: "button-edit-#{board.id}" do %>
              <i class="bi bi-pencil-fill"></i>
            <% end %>
            <%= link_to '#', id: "button-delete-#{board.id}", method: :delete, data: {confirm: ''} do %>
              <i class="bi bi-trash-fill"></i>
            <% end %>
          </div>
        </div>
        <ul class="list-inline">
          <li><i class="bi bi-person"></i><%= board.user.decorate.full_name %></li>
          <li><i class="bi bi-calendar"></i><%= l board.created_at, format: :short %></li>
        </ul>
        <p class="card-text">本文</p>
      </div>
    </div>
  </div>
</div>
```

### app/views/boards/index.html.erbを編集する

切り出したapp/views/boards/_board.html.erbをapp/views/boards/index.html.erbで表示させるために、`app/views/boards/index.html.erb`を以下のように編集してください。

- app/views/boards/index.html.erb

```
<div class="container pt-3">
  <div class="row">
    <div class="col-lg-10 offset-lg-1">
      <!-- 検索フォーム -->
      <form>
        <div class="input-group mb-3"><input class="form-control" placeholder="検索ワード" type="search"/>
          <div class="input-group-append"><input type="submit" value="検索" class="btn btn-primary"/></div>
        </div>
      </form>
    </div>
  </div>
  <!-- 掲示板一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @boards.present? %>
          <%= render @boards %>
        <% else %>
          <div class="mb-3">掲示板がありません</div>
        <% end %>
      </div>
    </div>
  </div>
</div>
```

< render @boards >は、collectionオプションを使ったrenderメソッドの記述を、省略記法を使って記述したものです。順序立てて説明するために、まずはcollectionオプションを使わずに記述したコードから説明します。

#### collectionオプションを使わずに記述した場合

```
<% @boards.each do |board| %>
  <%= render 'board', locals: { board: board } %>
<% end %>
```

上記のコードは、@boardsに含まれる各Boardオブジェクトを用いて以下のループ処理を行っています。renderで呼び出しているapp/views/boards/_board.html.erb内で使用されているboard変数（{ board: board }の左側）に、各Boardオブジェクト（{ board: board }の右側）を渡しています。

#### collectionオプションを使って記述した場合

```
<%= render partial: 'board', collection: @boards %>
```

partialには呼び出す部分テンプレートのファイル名を指定し、collectionには繰り返し表示するデータを指定します。この場合、部分テンプレートに渡る変数名はpartialで指定した値（今回でいうとboard）になります。collectionオプションを使う場合は、partialを明示的に記載しないとエラーが出るので注意してください。

#### collectionオプションを使った上で、省略記法を使って記述した場合

collectionオプションを使ったrenderメソッドの記述は、特定の条件を満たした場合にのみ、下記のように省略して記述することができます。

```
<%= render @boards %>
```

#### collectionの省略記法が使える条件

- 部分テンプレートが呼び出し元のテンプレートと同じディレクトリ内にある
- 部分テンプレートのファイル名が指定した変数の単数形である
- 部分テンプレート内で使用する変数名が、オプションで指定した変数の単数形である

## ■見た目・導線を整える

### app/views/shared/_header.html.erbを編集する

ログイン後のヘッダーから掲示板一覧ページへ遷移できるように`app/views/shared/_header.html.erb`を以下のように編集してください。

- app/views/shared/_header.html.erb

```
<header>
  <nav class='navbar navbar-expand-lg navigation navbar-light bg-light'>
    ... 省略 ...
    <div class='collapse navbar-collapse' id='navbarSupportedContent'>
      <ul class='navbar-nav ms-auto main-nav align-items-center'>
        <li class='nav-item dropdown dropdown-slide'>
          ... 省略 ...
          <div class='dropdown-menu dropdown-menu-end'>
            <%= link_to t('header.board_index'), boards_path, class: 'dropdown-item' %>
            <%= link_to t('header.create_board'), '#', class: 'dropdown-item' %>
          </div>
        </li>
        ... 省略 ...
      </ul>
    </div>
  </nav>
</header>
```

<%= link_to t('header.board_index'), boards_path, ... %> の行は、link_toヘルパーメソッドを使用して、掲示板一覧ページへのリンクを生成しています。t('header.board_index')は、国際化対応のために使用される翻訳キーで、config/locales/ja.ymlに定義されています。boards_pathは、掲示板一覧ページへのルーティングパスです。

### config/application.rbを編集する

`config/application.rb`を以下のように編集してください。

- config/application.rb

```
require_relative "boot"
... 省略 ...
module V3BasicRailsBasic
  class Application < Rails::Application
    ... 省略 ...
    config.i18n.default_locale = :ja
    config.time_zone = 'Tokyo'
  end
end
```

config.time_zone = 'Tokyo' は、デフォルトのタイムゾーンを日本時間に設定するための記述です。

### config/locales/views/ja.ymlを編集する

i18nを適切に表示するために`config/locales/views/ja.yml`を以下のように編集してください。

- config/locales/views/ja.yml

```
ja:
  defaults:
    flash_message:
      require_login: ログインしてください
  helpers:
    ... 省略 ...
  header:
    ... 省略 ...
  user_sessions:
    ... 省略 ...
  users:
    ... 省略 ...
```

### app/controllers/application_controller.rbを編集する

未ログイン時にログインページにリダイレクトさせた際に表示されるフラッシューメッセージを設定してください。`app/controllers/application_controller.rb`のrequire_loginメソッドを以下のように編集してください。

- app/controllers/application_controller.rb

```
class ApplicationController < ActionController::Base
  ... 省略 ...
  private

  def require_login
    redirect_to login_path, danger: t('defaults.flash_message.require_login') unless logged_in?
  end
end
```

### app/controllers/user_sessions_controller.rbを編集する

ログイン成功時に表示されるフラッシューメッセージを設定してください。`app/controllers/user_sessions_controller.rb`のcreateアクションを以下のように編集してください。

- app/controllers/user_sessions_controller.rb

```
class UserSessionsController < ApplicationController
  ... 省略 ...

  def create
    ... 省略 ...
    if @user
      redirect_to boards_path, success: t('user_sessions.create.success')
    else
      ... 省略 ...
    end
  end

  ... 省略 ...
end
```

## ■Boardレコードを用意する

掲示板一覧画面に表示する掲示板情報（Boardレコード）を用意してください。

### RailsコンソールでBoardレコードを生成する

Railsコンソールを立ち上げて、Boardレコードを1つ以上生成してください。Railsコンソールは以下のコマンドで立ち上げてください。

```
$ docker compose exec web rails c
```

Railsコンソールを立ち上げたら、以下を1行ずつ実行してBoardレコードを生成してください。

```
# Boardを投稿するUserレコードを生成します。
User.create(last_name: 'John', first_name: 'Doe', email: 'john_doe@example.com', password: 'password', password_confirmation: 'password')

# 上記で生成したUserに紐づいたBoardレコードを生成します。
Board.create(title: 'コンソールで作成', body: 'Rails基礎課題7取り組み中', user: User.last)
```

### db/seeds.rbを用いてレコードを生成する

Railsコンソールを使ってレコードを生成していくことも可能ですが、共同で開発する際にそうした手間は効率化させておくことが共同開発では大切になります。

以下db/seeds.rbを使用してレコードを生成する例を提示します。

#### db/seeds.rb

db/seeds.rbは、開発環境やテスト環境で使用するデータベースの初期データを設定するためのファイルです。このファイルに記述された内容は、rails db:seedコマンドを実行することでデータベースに適用されます。例えば、ユーザー情報やサンプルデータなど、アプリケーションの動作確認やテストに必要なデータを一括で生成することができます。これにより、開発者は手動でデータを入力する手間を省き、効率的に開発を進めることができます。

`db/seeds.rb`を以下のように記述してください。

- db/seeds.rb

```
10.times do
  User.create!(last_name: Faker::Name.last_name,
              first_name: Faker::Name.first_name,
              email: Faker::Internet.unique.email,
              password: "password",
              password_confirmation: "password")
end

user_ids = User.ids

20.times do |index|
  user = User.find(user_ids.sample)
  user.boards.create!(title: "タイトル#{index}", body: "本文#{index}")
end
```

このコードは、gem 'faker'を使用してランダムなユーザーと掲示板レコードを生成します。 まず、10.timesブロックで10人のユーザーを生成します。各ユーザーにはランダムな姓、名、ユニークなメールアドレス、そして固定のパスワードが設定されます。次に、全てのユーザーIDを取得し、user_ids配列に格納します。20.timesブロックでは、ランダムに選ばれたユーザーに関連付けられた掲示板レコードを20個生成します。各掲示板レコードには、タイトルと本文が設定されます。

gem 'faker'とは、ランダムなデータを生成するためのRubyライブラリです。このライブラリは、開発環境やテスト環境で使用するダミーデータを簡単に生成することができます。

上記コードを記載したらターミナルにて以下のコマンドを実行してください。

```
$ docker compose exec web rails db:seed
```

[^1]: 

[^2]: 
	

[^3]: 他に同じ値が存在しないこと
