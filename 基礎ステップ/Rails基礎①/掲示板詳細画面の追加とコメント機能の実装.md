
# own?メソッド
```ruby
def own?(object)
  id == object&.user_id
end
```

`own?` は

```このデータは自分のものか```

を判定するために作成したメソッドとなる

## ?で終わるメソッド
Rubyでは

```own?```

のように `?` で終わる名前を、真偽値を返すメソッドに使用することが多い

真偽値とは

```
true
false
```

の2種類を指す